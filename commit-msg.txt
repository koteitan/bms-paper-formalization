Close (ii) Suc k' k<t case via parameterized chain induction

- Add `keep_of_pre_strip_ge_max_parent_level`: strengthen the previous `0 < keep_of` bound to `t \<le> keep_of (G_block A @ Bs_concat A n)`, by exhibiting `bump_col A 0 1 ! m > 0` for every `m < t` (rows below the trim point cannot all be zero) and applying `keep_of_row_zero` for contradiction
- Add `m_parent_AEn_idx_B_within_block_at_Suc_k_when_k_lt_t`: parameterized helper that takes a `manc_inv` hypothesis (block-`c`-to-block-0 `m_ancestor` at `k'` equivalence) and concludes the within-block `m_parent` characterization at row `Suc k'`
- Add `m_parent_AEn_idx_B_outside_block_at_Suc_k_when_k_lt_t`: parameterized outside-block characterization with the same `manc_inv` hypothesis, derives the contradiction via `filter_empty_conv` (avoiding `m_ancestor.simps` auto-unfolding)
- Add `m_anc_idx_B_in_block_shift_at_Suc_k_when_k_lt_t`: chain induction that applies the helpers at `c = 0` (trivial `manc_inv`) and `c = n` (`manc_inv` from IH (ii) at `k'`)
- Dispatch the `lemma_2_5_ii_clause_step` Suc k' k<t sorry (BMS_Ancestry.thy:1844) via the chain-induction helper above
- Sorry count 9 -> 8
