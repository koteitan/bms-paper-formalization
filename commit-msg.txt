v0.1.28/restrict o_of_def to BMS arrays

- Add A : BMS premise to o_of_def axiom
- Unrestricted form was potentially inconsistent: arrays whose
  m_ancestor structure cannot be satisfied by any stable_rep admit
  no f, but the axiom unconditionally claimed existence
- For A : BMS, every A admits a stable_rep (provable by induction
  on BMS membership using stable_rep_extend), so the axiom is
  satisfiable on the intended domain
- Outside BMS, o_of A is unspecified; no proof reasons about it
- Update o_le_via_bms_le and o_preserves to discharge the BMS
  precondition via the already-available An_BMS = A[n] : BMS fact
- Bump version v0.1.27 -> v0.1.28
