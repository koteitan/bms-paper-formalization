v0.1.82/Reduce the mpl upper bound to a single no-parent obligation (downward closure)

- Add m_parent_Suc_imp_lower_parent: a (Suc k)-parent forces a k-parent (the candidate filter at level Suc k carries a k-ancestor guard, false without a k-parent), so the set of levels with a parent is downward closed
- Add mpl_lt_of_no_parent: if the last column has no k-parent, then mpl < k (descend from the achieved t-parent b0_start = m_parent A t C down to k via the downward closure)
- Reduces the mpl upper bound gate to a single value-side non-existence (no parent at one high level); no new sorry
- Bump version v0.1.81 -> v0.1.82

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
