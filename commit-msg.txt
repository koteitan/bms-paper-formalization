v0.1.29/seed_expansion_zero base lemmas + bug.md

- Add seed_expansion_zero: (seed N)[0] = [[]] for any N (the single
  all-zero column in butlast(seed N) strips to one empty column)
- Add seed_expansion_one_zero: (seed (Suc m))[1][0] = (seed (Suc m))[0]
  (base case k = 0 of the structural identity for seed expansion chain)
- Add bug.md: tracker for Hunter paper errors found during the audit
  - B-1: Lemma 2.3 proof's "A[0] = A without last column" is literally
    inconsistent with the strip in Definition 1.1 (concrete counter:
    seed N for N >= 1); workaround = use the strip-respecting identity
    (A[k+1])[0] = A[k] instead
- Bump version v0.1.28 -> v0.1.29
